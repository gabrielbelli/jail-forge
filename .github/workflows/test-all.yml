name: "Test: All Modes"

on:
  workflow_dispatch:
    inputs:
      skip_destroy:
        description: 'Skip final cleanup'
        required: false
        type: boolean
        default: false

jobs:
  test:
    uses: ./.github/workflows/test-lifecycle.yml
    with:
      test_alias_mode: true
      test_nat_mode: true
      test_vnet_mode: true
      test_inherit_mode: true
      skip_destroy: ${{ inputs.skip_destroy }}
    secrets: inherit
